pipeline {
    agent {
        docker { image 'jenkins/agent:latest' }
    }
    
    stages {
        stage("Test") {
            steps {
                sh 'node --version'
                sh 'whoami'
                sh 'pwd'
                sh 'hostname'
            }
        }
    }
}
