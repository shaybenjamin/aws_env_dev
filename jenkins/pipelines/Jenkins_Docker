pipeline {
    agent {
        label 'slave'
    }
    
    stages {
        stage("Test") {
            steps {
                sh 'node --version'
                sh 'whoami'
                sh 'pwd'
                sh 'hostname'
            }
        }
    }
}
