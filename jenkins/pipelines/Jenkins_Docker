pipeline {
    agent {
        docker { image 'jenkins/inbound-agent:latest' }
    }
    
    stages {
        stage("Test") {
            steps {
                sh 'node --version'
                sh 'whoami'
                sh 'pwd'
                sh 'hostname'
            }
        }
    }
}
