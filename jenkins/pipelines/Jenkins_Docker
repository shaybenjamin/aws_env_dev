pipeline {
    agent { node { label 'jenkins-agent' } }
    stages {
        stage("Test") {
            steps {
                sh 'node --version'
                sh 'whoami'
                sh 'pwd'
                sh 'hostname'
            }
        }
    }
}
