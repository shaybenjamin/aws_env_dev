pipeline {
    agent { node { label 'jenkins-agent' } }
    stages {
        stage("Test") {
            steps {
                sh 'whoami'
                sh 'pwd'
                sh 'hostname'
            }
        }
    }
}
